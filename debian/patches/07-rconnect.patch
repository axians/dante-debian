--- a/lib/Rconnect.c	2008-07-07 18:22:06.000000000 +0300
+++ b/lib/Rconnect.c	2008-07-07 18:22:10.000000000 +0300
@@ -75,6 +75,10 @@
 		return connect(s, name, namelen);
 	}
 
+#if SOCKS_CLIENT /* may be called before normal init, log to right place */
+	clientinit();
+#endif     
+   
 	slog(LOG_DEBUG, "%s: %s",
 	function, sockaddr2string(name, namestring, sizeof(namestring)));
 
